import React from 'react';
import logo from '../../images/logo.png';

const Logo = (height) => {
  return (
    <div className="logo">
      <img src={logo} alt="logo_SmartQube" />
    </div>
  );
};

export default Logo;
